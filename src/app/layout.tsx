import type { Metadata } from 'next';
import { Inter, Playfair_Display } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-playfair',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'The Infinite Canopy — A Journey Through the Universe',
  description: 'A cinematic voyage into the cosmos. Explore stellar nurseries, the geometry of spacetime, and the infinite wonders of the universe.',
  openGraph: {
    title: 'The Infinite Canopy',
    description: 'A cinematic voyage into the cosmos.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body>{children}</body>
    </html>
  );
}
